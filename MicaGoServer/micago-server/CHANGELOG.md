# Changelog

## v0.1.1

- Default API view now presents a clean iMessage-only chat and recent-message view
- Added `service` filtering for `iMessage`, `SMS`, `RCS`, and `all`
- Added `includeEmpty` support for message endpoints
- Fixed live Apple epoch timestamp conversion for nanosecond-scale values in `chat.db`
- Added local smoke and `chat.db` inspection scripts for live verification
